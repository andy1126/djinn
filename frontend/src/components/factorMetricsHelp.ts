/** 因子分析 / 多因子诊断共用的指标含义与 IC 区间参考。 */

/** IC 区间经验参考(秩相关、截面)。 */
export const IC_RANGE_HELP = [
  { range: '|IC| < 0.03', meaning: '基本无选股能力,接近噪声' },
  { range: '0.03 ≤ |IC| < 0.05', meaning: '弱,有一定预测力' },
  { range: '0.05 ≤ |IC| < 0.10', meaning: '中等,较有效的因子' },
  { range: '0.10 ≤ |IC| < 0.20', meaning: '强(需排除过拟合/未来函数)' },
  { range: '|IC| ≥ 0.20', meaning: '极强,高度警惕过拟合' },
]

/** 各指标含义(悬停 tooltip 与「指标说明」面板共用)。 */
export const METRIC_TIP: Record<string, string> = {
  'IC 均值': '信息系数,因子值与未来收益的秩相关(Spearman)。正=因子值越大收益越高,负=相反。区间含义见「指标说明」。',
  ICIR: 'IC 均值 ÷ IC 标准差,衡量信号稳定性:>0.5 稳定,0.3~0.5 一般,<0.3 不稳定。',
  'IC 正值占比': '交易日中 IC>0 的天数占比。越接近 50% 越随机;>55% 方向较稳,>60% 很稳。',
  't 值': 'Newey-West(HAC)标准误下的 IC 均值 t 统计量,衡量 IC 显著区别于 0 的程度。|t|>2 通常认为显著(p<0.05)。',
  单调性: '分层收益是否随因子值单调变化(0~1)。1=完全单调,越高说明「因子值→收益」阶梯越清晰。',
  换手: '因子排序逐日变化程度(rank turnover)。≈0 为慢变量(基本面,两期之间不动);高为快变量(量价)。',
  'λ 均值': 'Fama-MacBeth 逐日截面回归得到的因子风险溢价(λ)时间序列均值,即该因子平均每期贡献的超额收益。',
  'λ t 值': '因子风险溢价 λ 时序的 Newey-West t 值,衡量溢价显著区别于 0 的程度。|t|>2 通常认为显著。',
  '建议调仓频率': '按 IC 衰减曲线推荐:找到 IC 衰减到峰值 50% 的最短持有期,映射到最近调仓档(daily/weekly/monthly/quarterly)。无有效峰值(如 IC 长期不衰减)则无推荐。',
}

/** 多因子诊断相关矩阵说明(因子间截面相关,判断冗余,不是 IC)。 */
export const CORR_MATRIX_TIP =
  '因子两两之间的截面相关(逐日截面相关跨日均值),用于诊断因子是否冗余(常 >0.8 即高度冗余)。注意:这是「因子 vs 因子」的相关,不是 IC(IC 是「因子 vs 前向收益」)。'
